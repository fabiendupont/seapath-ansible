---
galaxy_info:
  author: "Seapath"
  description: "Pacemaker resource manager and cluster management"
  license: Apache-2.0
  min_ansible_version: 2.9.10
  platforms:
    - name: EL
      versions: [all]
    - name: Debian
      versions: [all]
    - name: Yocto
      versions: [all]
  galaxy_tags:
    - cluster
    - pacemaker
    - high-availability
    - seapath
dependencies:
  - role: cluster_corosync
