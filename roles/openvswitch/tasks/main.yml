# Copyright (C) 2025 RTE
# SPDX-License-Identifier: Apache-2.0

---
# Include OS family-specific variables
- include_vars: "{{ ansible_os_family }}.yml"

# OVS configuration
- name: Configure Open vSwitch (Native Ansible)
  include_tasks: ovs_native.yml

# OVS service hardening
- name: Apply OVS service hardening
  include_tasks: ovs_hardening.yml

# Cluster network configuration (HSR/RSTP)
- name: Configure cluster network
  include_tasks: cluster_network.yml
